# Аудит производительности и стабильности сети

Дата: 2026-08-06 · ветка `claude/game-audit-ping-stability-ewz7tl` (от `d723e93`)
Повод: жалобы на **высокий пинг у части игроков**, «моменты нестабильности», лаги.
Объём: `server/index.js`, `server/game/Room.js`, `shared/netcodec.js`, клиентский
цикл (`js/game.js`, `js/network.js`), транспорт socket.io, зависимости.

Предыдущий аудит (`AUDIT.md`, 2026-08-04) закрывал **корректность и безопасность**.
Этот — только **латентность, пропускная способность и стабильность соединения**.

> **Статус: всё из списка ниже, кроме P0-3 и P2-7, исправлено.** Итоговые
> замеры «было → стало» — в разделе [«Что сделано»](#что-сделано) в конце.
> Коротко: цена визуалов боя упала в 6.3 раза, наплыв входов больше не роняет
> тик (125 мс → 16 мс), карта при переподключении не скачивается вовсе.

> Всё, что помечено **«замер»**, измерено на живом сервере (`npm run dev:local`,
> тот же `server/index.js`, что и на проде) синтетическими клиентами через
> socket.io. Стенд: 4 ядра, Node 22, боты на той же машине. CPU сервера снят из
> `/proc/<pid>/stat`, тайминги тика — из `/health`, RTT — со стороны клиента.
> Реальной сети между ботами и сервером нет, поэтому **абсолютные RTT ниже
> реальных**; значимы не они, а разница между сценариями.

---

## Сводка

| # | Находка | Влияние | Где |
|---|---|---|---|
| **P0-1** | `spawnProj`/`spawnAoe`/`enemyCC` рассылаются **всему этажу** — стоимость растёт как N² | **+37% ядра на 150 игроках** при 2 выстрелах/с; на 300 — насыщение | `server/index.js:5275, 5544, 5559` |
| **P0-2** | Одновременный вход толпы (после каждого рестарта/деплоя) кладёт тик | tickMax **125 мс** (×5 бюджета), RTT p99 **146 мс** против 26 мс | `server/index.js:4924` (`selectChar`) |
| **P0-3** | Один процесс = одно ядро; потолок ~250–300 онлайн, метрики ёмкости нет | 150 игроков = **52% ядра** уже без визуалов боя | архитектура |
| **P1-1** | `playerMove` — 40 Гц, шлётся даже стоя на месте, не volatile | **18.5% ядра** на 150 игроках только на приём | `js/game.js:269`, `js/network.js:1578` |
| **P1-2** | `gameState` шлётся 20×/с даже когда пакет пустой; каждый — **2 WS-кадра**, 79% байт — обёртка socket.io | 40 кадров/с в никуда у одиночного игрока | `Room.js:962`, `netcodec.js` |
| **P1-3** | `pingInterval 30s` / `pingTimeout 90s` — обрыв связи замечается до **120 с** | зависший мир на 2 минуты вместо переподключения | `server/index.js:874` |
| **P1-4** | Полная карта (132 КБ) пересылается на **каждый реконнект**, клиент каждый раз перестраивает мир | мобильный игрок платит 132 КБ за каждый блип связи | `server/index.js:4971`, `js/network.js:211` |
| **P1-5** | `enemyResync` в «быстрой» корзине лимитера — усиление 1 : 40 | один клиент вытягивает 12 000 полных записей/с | `server/index.js:4502, 3546` |
| **P1-6** | `/bundle.js` сжимается заново каждому клиенту, ETag меняется на каждом рестарте | 1.07 МБ → 301 КБ, ~30–50 мс CPU на клиента | `server/index.js:1638` |
| **P2-1** | `mapBlips` шлёт всех ~7100 мобов, включая 3600 в Башне, которых получателю видеть нельзя | 14 КБ/с на смотрящего карту вместо ~4 | `Room.js:1203` |
| **P2-2** | `io.to(id).emit('playerHurt')` внутри AI-тика | лишний BroadcastOperator на каждый удар моба | `Room.js:778` |
| **P2-3** | `viewersOfEnemy` — O(всех игроков) на каждый удар, с аллокацией массива | 450k проверок/с на 300 игроках | `Room.js:1088`, `index.js:3345` |
| **P2-4** | `encodeGameState` возвращает новый `ArrayBuffer` каждому игроку каждый каст | 6000 аллокаций/с на 300 игроках | `netcodec.js:200` |
| **P2-5** | Нет метрики лага event loop; окно `/health` обнуляется при чтении | деградация не видна, пока не пожалуются | `server/index.js:1576` |
| **P2-6** | `_getRoomAt` — линейный перебор 469 комнат, несколько раз за кадр | лишние ~100k итераций/с на телефоне | `js/game.js:184` |
| **P2-7** | `chat` → `io.emit` всем | до 30 000 отправок/с в пике на 300 онлайн | `server/index.js:5594` |
| **P2-8** | Мелкие несбрасываемые Map по `telegramId` | рост памяти за время аптайма | `server/index.js:979, 1010` |

Уязвимостей в зависимостях нет: `npm audit --omit=dev` → **0 vulnerabilities**
(socket.io 4.8.3, ws 8.21.1, express 4.22.2, mongoose 8.24.1).

---

## Что вообще меряет «пинг» в игре

Клиент показывает `_pingMs` из своего же цикла `_ping`/`_pong` (`js/network.js:87`).
Это **не чистая сеть**: пакет попадает в тот же event loop, что и мировой тик, и
в тот же главный поток браузера, что и рендер. Поэтому «высокий пинг» у игрока
складывается из трёх независимых слагаемых:

1. реальная сеть (география, мобильный радиоканал) — не в нашей власти;
2. **очередь на сервере** — если тик занял 25 мс, ответ на `_ping` ждёт эти 25 мс;
   именно это раздувают находки P0-1…P0-3;
3. **зависание главного потока у самого игрока** — GC, декодирование спрайтов,
   перестройка мира после реконнекта (P1-4). Телефон, простоявший 120 мс в GC,
   покажет это как «пинг 120».

Отсюда и «у некоторых игроков» — слагаемые 2 и 3 бьют по игрокам неравномерно:
в толпе (хаб, босс, ивент) пакет больше и работы на игрока больше, а слабый
телефон добавляет своё сверху.

---

## P0-1. Визуалы боя рассылаются всему этажу — главный источник лага

`spawnProj` (каждый выстрел лучника/мага), `spawnAoe` и `enemyCC` уходят
**каждому подключённому игроку**, а не тем, кто может это увидеть:

```js
// server/index.js:5544
safeOn('spawnProj', data => {
  if (!currentRoom || !data || typeof data !== 'object') return;
  socket.to(`floor_${currentFloor}`).emit('spawnProj', { ... });   // ← весь этаж
});
```

Мир — один общий этаж (`MAX_FLOOR = 1`), так что «этаж» = «все онлайн».
`fireProj` (`js/combat.js:52`) вызывается на **каждой** дальней автоатаке, скилы
лучника шлют по 3 стрелы за нажатие. То есть каждый стрелок генерирует 1–3
широковещательных пакета в секунду, а стоимость каждого — количество игроков
онлайн. Итог квадратичный: `игроки × выстрелы × игроки`.

**Замер** (150 ботов, движение 40 Гц, без атак; сравниваются 0 и 2 выстрела/с):

| Сценарий | CPU сервера за 20 с | Доля ядра |
|---|---|---|
| без `spawnProj` | 11.07 с | 55% |
| 2 `spawnProj`/с на бота | **18.53 с** | **93%** |

Одни только визуалы снарядов добавили **7.46 с CPU за 20 с — +37% целого ядра**,
то есть **+67% ко всей остальной работе сервера**, при том что это чистая
косметика: урон идёт отдельным `attack`/`skillAttack`.

Пересчёт: 150 × 2 = 300 рассылок/с × 149 получателей ≈ 44 700 отправок/с ≈ 8.3 мкс
на получателя. На 300 игроках при том же поведении — 600 × 299 ≈ 179 000 отправок/с,
что **больше секунды CPU на секунду времени**: сервер перестаёт успевать, и лаг
получают все, а не только стрелки.

**Что делать.** Ограничить радиусом видимости — ровно так же, как уже сделано
для `enemyHurt`/`enemyKilled` (`_emitToEnemyViewers`, `index.js:3361`). Снаряд
чужого игрока за пределами `PLAYER_AOI_R` (600 px) не видно в принципе: самого
стрелка клиенту уже не передают. `Room` держит `_playerGrid`, поэтому нужен
только метод «id игроков в радиусе R от точки», и рассылка сокращается с N до
≤20 получателей. То же самое для `enemyCC` (`index.js:5275`) и `pvpPlayerCC`
(`index.js:5361`).

---

## P0-2. Толпа входит одновременно — тик встаёт на 125 мс

Каждый вход (`selectChar`) синхронно собирает и сериализует полное описание мира:

```
gridPacked   52 674 байт (упакованная сетка 254×1659)
rooms        74 372 байт JSON (469 комнат)
остальное     ~4 800 байт
итого        ~132 КБ на одного входящего
```

`JSON.stringify` этого блока — **0.41 мс** на вход (замер, 200 итераций), плюс
чтение из Mongo, плюс `enemySnapshot`, плюс кодирование 52 КБ бинарного
вложения. Всё это выполняется в том же потоке, что и мировой цикл.

**Замер** — 150 клиентов, одинаковая нагрузка, разница только в темпе входа:

| Вход | tickMsMax | Превышений бюджета | RTT p90 | RTT p99 | RTT max |
|---|---|---|---|---|---|
| по 25 мс (штатно) | 27 мс | 2 | 16 мс | 26 мс | 36 мс |
| все сразу | **125 мс** | **19** | 20 мс | **146 мс** | **262 мс** |

Пять бюджетов тика в одном кадре — это видимый рывок у **всех**, кто в этот
момент играет. И это не редкий случай: так выглядит **каждый деплой и каждый
рестарт** — socket.io переподключает всех в окне 0.5–1.5 с
(`reconnectionDelay` 1 с, `randomizationFactor` 0.5).

**Что делать.**
1. Не пересылать карту тем, у кого она уже есть: отдать её отдельным HTTP-ресурсом
   с `ETag`/`immutable` (она неизменна всё время жизни процесса) и держать в
   `gameStart` только `mapVersion`. Реконнект тогда не стоит почти ничего.
2. Заранее сериализовать `dungeonData` один раз (`Buffer`), а не собирать JSON
   каждому входящему.
3. Ограничить приём входов: не больше N `selectChar` за тик, остальные — в
   очередь на следующий (игрок увидит +25–50 мс к загрузке, зато никто не увидит
   рывок).

---

## P0-3. Одно ядро на весь мир, и потолок не измеряется

Весь мир, все сокеты и весь ввод-вывод живут в одном процессе Node — то есть на
одном ядре, сколько бы их ни было на машине.

**Замер** (150 игроков, все в хабе, 40 Гц движения, без снарядов):

| Что | Значение |
|---|---|
| CPU сервера | 52% ядра |
| tick p50 / max | 6.4 мс / 22 мс (бюджет 25 мс) |
| приём `playerMove` | 18.5% ядра |
| остальное (тик + рассылка) | 34% ядра |

Изолированный бенчмарк `Room._tick()` без сокетов (`dev/roombench.js`), 300
игроков в одной точке фарма: каст-тик p50 **11.4 мс**, p90 15.3 мс, то есть
**половина бюджета — до** того, как socket.io что-либо отправит.

Профиль CPU живого сервера под 200 ботами (`node --cpu-prof`, самое дорогое):

```
14.7%  writev                     ← системный вызов записи в сокеты
 5.8%  Room.js:852                ← поцикловый сбор AOI на каждого игрока
 1.9%  encodeGameState
 1.6%  Room._tick
 1.3%  socket.io-parser tryParse   ← разбор входящих playerMove
 1.0%  garbage collector
```

Главная статья — **не расчёт мира, а запись в сокеты**. Значит, выигрыш даёт
сокращение **числа пакетов**, а не их размера (P0-1, P1-1, P1-2).

Линейная экстраполяция: ~250–300 одновременных игроков — предел одного ядра
**без** учёта P0-1; с текущей рассылкой снарядов реальный предел заметно ниже.

**Что делать (по возрастанию сложности).** Сначала P0-1/P1-1/P1-2 — они дают
кратный запас без изменения архитектуры. Дальше, если онлайн растёт: вынести
мировой цикл в `worker_thread` (сокеты остаются в главном) либо шардировать мир
по рукавам между процессами с `@socket.io/cluster-adapter` и sticky-сессиями.

---

## P1-1. `playerMove` — 40 Гц, всегда, даже стоя на месте

`netSendMove()` вызывается безусловно в каждом шаге `update()` (`js/game.js:269`)
и ограничен только троттлом 25 мс — то есть 40 пакетов/с, пока клиент запущен,
независимо от того, двигался игрок или стоит в меню, на рынке, в чате.

Три следствия:
* сервер рассылает мир **20 раз/с** (`castPlayers` — каждый второй тик), так что
  половина входящих пакетов физически ни на что не влияет;
* на мобильном это постоянные пробуждения радио — расход батареи и, что важнее,
  задержка на самом канале;
* пакет **не volatile**: на подвисшем канале они копятся и потом приходят пачкой,
  после чего персонаж «телепортируется» — ровно то, что игроки описывают как лаг.

**Замер** (150 ботов, без атак и снарядов):

| Темп `playerMove` | CPU сервера за 20 с |
|---|---|
| 40 Гц | 10.50 с |
| 20 Гц | 9.24 с |
| не шлём вовсе | 6.83 с |

Приём позиций — **3.7 с из 20 (18.5% ядра)** на 150 игроках; на 300 это ~37%
ядра, при том что большая часть пакетов повторяет предыдущий.

**Что делать.** Слать при изменении (сдвиг > ~0.5 px, смена `facing`, изменение
hp) с потолком 20 Гц и «сердцебиением» 1 Гц, и через `socket.volatile`.
Стоящий в меню игрок перестанет генерировать трафик вообще.

---

## P1-2. Пустые пакеты мира и оверхед socket.io: 79% байт — обёртка

`_tick` отправляет `gameState` **всегда**, даже когда и список игроков, и список
мобов пусты (`Room.js:962`) — а это норма для одиночного игрока в коридоре, для
AFK в хабе, для любого, кто открыл рынок.

**Замер** одного изолированного клиента, 10 секунд:

```
событий gameState        199        (≈20/с)
пакетов engine.io        397        → ровно 2 WebSocket-кадра на событие
байт заголовков          9 108      ← 451-["gameState",{"_placeholder":true,"num":0}]
байт полезных данных     2 467
доля обёртки             78.7%
```

То есть на пустом потоке игрок получает **40 кадров в секунду**, из которых
четыре пятых объёма — служебная строка socket.io. Каждый кадр — отдельная запись
в сокет (`writev`, 14.7% CPU по профилю) и отдельное пробуждение радио у клиента.

**Что делать.**
1. Не отправлять каст, если оба списка пусты **и** предыдущий каст тоже был пуст
   (один пустой пакет всё же нужен — по нему клиент вычищает ушедших игроков);
   раз в секунду — «сердцебиение» для синхронизации часов (`_svrTimeOffset`).
2. Убрать второй кадр: либо msgpack-парсер (бинарник кладётся в тот же кадр,
   меняется одной опцией на сервере и клиенте, но требует вендорить парсер к
   socket.io-клиенту), либо свой канал поверх engine.io с однобайтовым тегом
   события. Экономия — половина кадров и ~80% байт этого потока.

---

## P1-3. Обрыв связи замечается до двух минут

```js
// server/index.js:874
pingTimeout: 90000,
pingInterval: 30000,
```

engine.io-клиент считает соединение живым, пока не пройдёт
`pingInterval + pingTimeout` = **120 с** без пинга. Пока таймер идёт:

* игрок видит **замерший мир** и не понимает, что уже отключён — переподключение
  даже не начинается;
* на сервере остаётся «призрак»: по нему бьют мобы, его видно в PvP, он занимает
  слот `activeSessions`;
* в его мёртвый сокет продолжают литься 20 пакетов/с.

Для мобильного клиента (переключение Wi-Fi ↔ LTE, засыпание радио, туннель в
Telegram WebView) это типовой сценарий: TCP не рвётся, а «чернеет», и обнаружить
это может только heartbeat.

**Что делать.** `pingInterval` 15 с, `pingTimeout` 20–25 с (обнаружение ≤ 40 с).
Дополнительно — сторожевой таймер на клиенте поверх уже существующего цикла
`_ping` каждые 2 с: нет `_pong` дольше 6 с → принудительный
`socket.disconnect(); socket.connect()`. Это возвращает игрока в игру за
секунды, а не за две минуты.

---

## P1-4. Каждый реконнект — 132 КБ и полная перестройка мира на клиенте

`connect` → `loginTelegramWebApp` → `authOk` → `netSelectChar(...)`
(`js/network.js:145`) выполняется **на любом** переподключении socket.io, а
серверный `selectChar` в ответ всегда шлёт полный `gameStart` с картой.

Клиент на это отвечает (`js/network.js:211`): `unpackGrid` на 421 486 клеток
(1659 отдельных `Uint8Array`), `_buildArmGates`, `buildTileCanvas`, сброс всех
буферов, перезагрузка спрайтов. На телефоне это десятки-сотни миллисекунд
замершего кадра — и они честно попадут в показанный «пинг».

Карта при этом **одна и та же** для всех игроков и не меняется до рестарта
процесса.

**Что делать** — то же, что в P0-2: отдавать карту как кэшируемый HTTP-ресурс
(или хотя бы не слать `dungeon` в ветке `_isReconnectRejoin`, клиент её уже
держит в памяти). Реконнект после блипа связи должен стоить один маленький
пакет, а не 132 КБ и перестройку мира.

---

## P1-5. `enemyResync` — усиление запроса в 40 раз

`enemyResync` не входит в `_HEAVY_EVENTS` (`server/index.js:3527`), значит
попадает в «быструю» корзину: **1500 событий за 5 с = 300/с** на сокет. Каждый
запрос — до `ENEMY_RESYNC_MAX = 40` **полных** записей о мобах (со строками:
id, eid, имя, цвет), кодирование и отдельный пакет:

```js
// server/index.js:4502
safeOn('enemyResync', ({ ids } = {}) => { ... currentRoom.resendEnemies(socket.id, ids); });
```

Один модифицированный клиент вытягивает до 12 000 полных записей в секунду —
дешёвый запрос, дорогой ответ. Штатный клиент шлёт максимум 2 запроса/с
(`_ENEMY_RESYNC_MS = 500`), так что запас можно резать смело.

**Что делать.** Отдельная корзина на 5 запросов/с (или добавить в `_HEAVY_EVENTS`,
там 40 за 5 с = 8/с — тоже достаточно).

---

## P1-6. Бандл пережимается для каждого клиента

`/bundle.js` — 1 096 502 байта, gzip 301 155 (замер). Он собирается один раз при
старте, но `compression()` сжимает его **заново на каждый запрос** (~30–50 мс CPU
в пуле libuv), а `ETag` меняется при каждом рестарте, поэтому после деплоя за
бандлом приходят все.

**Что делать.** Сжать один раз при сборке бандла и отдавать готовый буфер с
`Content-Encoding: gzip` (плюс brotli, если есть). ETag уже считается от
содержимого — стоит сделать его стабильным между рестартами (он и так
`sha1` от текста; сейчас содержимое не меняется, а вот заголовки кэша
`no-cache` заставляют ходить за 304 при каждой загрузке).

---

## P2 — заметное, но не определяющее

**P2-1. `mapBlips`.** `_broadcastMapBlips` (`Room.js:1203`) кладёт в пакет всех
живых не-боссов — это ~7100 существ, из которых 3600 — монстры коридоров Кровавой
Башни, которых получателю показывать нельзя в принципе (`_raceVisible`). 14 КБ/с
вместо ~4 КБ/с, каждую секунду, каждому, кто открыл карту. Фильтровать по руке и
дорожке.

**P2-2. `playerHurt` в AI-цикле.** `Room.js:778` использует
`this.io.to(closest.socketId).emit(...)` — на каждый удар моба создаётся
`BroadcastOperator` и `Set` комнат, ровно та лишняя работа, которую соседний код
уже избегает через `_socketFor(p)`. Заменить на `sock.emit`.

**P2-3. Веер боевых событий.** `viewersOfEnemy` (`Room.js:1088`) перебирает
**всех** игроков комнаты на каждый удар, а `_enemyViewers` (`index.js:3345`)
аллоцирует новый массив. При 300 игроках и 5 ударах/с это ~450 000 обращений к
Map и 1500 массивов в секунду. Обратный индекс «моб → зрители» (обновляется там
же, где `_eKnown`) убирает и то, и другое.

**P2-4. Аллокации кодека.** `encodeGameState` возвращает `_ncBuf.slice(0, o)` —
свежий `ArrayBuffer` на каждого получателя каждый каст (6000/с при 300 игроках).
В бенчмарке это Scavenge каждые ~80 мс по 0.6–1.2 мс. Копия нужна (socket.io
держит буфер до отправки), но кольцевой пул буферов её бы снял.

**P2-5. Нет метрики ёмкости.** `/health` отдаёт тайминги тика только админу и
**обнуляет окно при чтении** — то есть два монитора мешают друг другу, а истории
нет вообще. Нужны: счётчик лага event loop, лог-строка при `tickMs > бюджет`
(с троттлом) и внешний сбор метрик. Сейчас «сервер тупит» невозможно ни
подтвердить, ни опровергнуть постфактум.

**P2-6. `_getRoomAt`** (`js/game.js:184`) — линейный перебор 469 комнат, вызывается
несколько раз за кадр (`game.js:408, 435`, `ui.js:910, 1437`). ~100k итераций/с на
телефоне. Индекс «тайл → комната» строится один раз при `gameStart`.

**P2-7. `chat`** → `io.emit` всем (`index.js:5594`). Лимит 1 сообщение/3 с на
игрока, но при 300 онлайн это до 100 сообщений/с × 300 получателей = 30 000
отправок/с в пике. Чат глобален по замыслу — но его стоит буферизовать и слать
пачкой раз в ~200 мс.

**P2-8. Мелкие утечки.** `_logWritesSinceTrim` (`index.js:979`) и
`_pvpHistoryWritesSinceTrim` (`index.js:1010`) — Map по `telegramId`, из которых
записи не удаляются никогда: растут с числом уникальных аккаунтов за аптайм.
Единицы мегабайт за месяцы, но чистить при disconnect тривиально.

---

## Что сделано

Все цифры ниже — замеры на том же стенде и теми же командами, что и в разделах
выше: 150 синтетических клиентов, CPU сервера из `/proc`, тайминги тика из
`/health`, RTT со стороны клиента.

### Главное

| Что мерим | Было | Стало | Раз |
|---|---|---|---|
| **Цена визуалов боя** (150 игроков × 2 снаряда/с) | 7.46 с CPU / 20 с — **37% ядра** | 1.18 с — **6% ядра** | **6.3×** |
| CPU целиком, тот же сценарий | 18.53 с — 93% ядра | 11.20 с — **56% ядра** | 1.7× |
| **Наплыв 150 входов**: tickMax | **125 мс** | **16 мс** | 7.8× |
| — превышений бюджета тика | 19 | **0** | — |
| — RTT p99 / max | 146 / 262 мс | **19 / 30 мс** | 7.7× / 8.7× |
| **Карта при переподключении** | 132 КБ и полная перестройка мира | **0 байт** (проверено в браузере) | ∞ |
| Пакетов `gameState`/с одинокому игроку | 20.3 | **2.8** | 7.3× |
| Лента точек карты, игрок в руке | 27.8 КБ/с | **2.9–3.7 КБ/с** | 8× |
| — игрок в Кровавой Башне | 27.8 КБ/с | **0.5 КБ/с** (своя дорожка) | 56× |
| 20 загрузок `/bundle.js` | 1.03 с | **0.21 с** | 5× |
| Обнаружение обрыва связи | до **120 с** | **≤40 с** протоколом, **~8 с** сторожем | 15× |

### Приём позиций (`playerMove`), 150 игроков, 20 с CPU

| Клиент | Все двигаются | Половина стоит |
|---|---|---|
| было: 40 Гц всегда | 11.07 с (55% ядра) | 11.07 с (55%) — стоящие слали столько же |
| стало: ≤20 Гц по изменению | **8.86 с (44%)** | **8.47 с (42%)** |

### Итог по нагрузке: 200 игроков в хабе

Строка «было» — **без** снарядов (со снарядами такой замер до исправления
просто не выдерживал сравнения), строка «стало» — **со** снарядами, атаками и
30% стоящих игроков:

| | tickMsAvg | tickMsMax | превышений | RTT p90 / p99 / max |
|---|---|---|---|---|
| было (без снарядов) | 8.7–10.2 мс | 35–39 мс | 7–27 | 22 / 35 / 53 мс |
| стало (со снарядами) | **8.26 мс** | **30 мс** | **5** | **19 / 26 / 48 мс** |

### Потолок онлайна, пересчёт

Цена одной отправки та же (8.3 мкс), изменилось число получателей: не «все
онлайн», а не более 24 в радиусе 1000 px.

| Онлайн, все стреляют 2/с | Было | Стало |
|---|---|---|
| 150 | 93% ядра | ~56% ядра |
| 300 | **149% — не успевает** | ~12% ядра на снаряды + база |
| 500 | **415% — не успевает** | снаряды перестают быть значимой статьёй |

### Список изменений

| # | Что сделано | Файлы |
|---|---|---|
| P0-1 | `spawnProj`/`spawnAoe`/`enemyCC`/`pvpPlayerCC` — рассылка по радиусу 1000 px через игровую сетку, с потолком 24 получателя (в давке радиус ничего не ограничивает) и фильтром по дорожке Башни | `Room.js` (`nearbyPlayerIds`), `index.js` (`_emitNearby`) |
| P0-2 / P1-4 | Карта вынесена из `gameStart` в `/api/world-map/<hash>` с `immutable`-кэшем; мир генерируется от фиксированного сида, поэтому хеш стабилен между рестартами. Клиент держит её в памяти и не перезапрашивает при переподключении; при неудаче HTTP — резерв через сокет | `Room.js` (`mapPayload`), `index.js`, `js/network.js` |
| P1-1 | `playerMove` — ≤20 Гц, только при реальном изменении, `volatile`, keepalive 1 Гц | `js/network.js` |
| P1-2 | Пустые касты не отправляются; один финальный пустой пакет (чтобы клиент вычистил ушедших) плюс «сердцебиение» раз в секунду для синхронизации часов | `Room.js` |
| P1-3 | `pingInterval` 30→15 с, `pingTimeout` 90→25 с; сторожевой таймер на клиенте: нет `_pong` 8 с → переподключение | `index.js`, `js/network.js` |
| P1-5 | Третья корзина лимитера для усиливающих событий: `enemyResync` и `worldMapInline` — 10 за 5 с вместо 1500 | `index.js` |
| P1-6 | Бандл сжимается один раз при старте | `index.js` |
| P2-1 | Точки карты — по рукам, а внутри Башни по дорожкам, и только тем, кто в руке | `Room.js` |
| P2-2 | `playerHurt` в AI-цикле — прямо в сокет жертвы вместо `io.to(id)` | `Room.js` |
| P2-6 | `_getRoomAt` помнит прошлый ответ (комната почти всегда та же) | `js/game.js` |
| P2-8 | `_logWritesSinceTrim` / `_pvpHistoryWritesSinceTrim` чистятся при отключении | `index.js` |

Не делалось: **P0-3** (вынести мир в worker / шардировать) — после P0-1 запас
есть, и это единственный пункт, меняющий архитектуру; **P2-3/P2-4/P2-5/P2-7**
(обратный индекс зрителей, пул буферов кодека, метрика event loop, батчинг
чата) — по замерам это уже не значимые статьи расхода.

### Чем проверено

* `dev/fanout-check.js` — соседний игрок получает все 10 снарядов и 10 AOE,
  игрок в другой руке — ноль. Оптимизация никому не «съела» эффекты.
* `dev/smoke.js` — реальный клиент в headless-браузере: логин, выбор класса,
  движение, **переподключение без повторной загрузки карты**
  (`refetchedMap: 0`), мир на месте, ошибок в консоли нет. Тот же скрипт на
  коде до изменений даёт идентичный скриншот.
* Полный прогон нагрузки с боем, снарядами и открытой картой — в логе сервера
  ноль ошибок обработчиков.

---

## Порядок работ (исходный план)

Ниже — план, по которому шла работа; фактические результаты в разделе выше.

| Приоритет | Что | Ожидаемый эффект |
|---|---|---|
| 1 | P0-1: AOI для `spawnProj`/`spawnAoe`/`enemyCC` | снимает главный квадратичный расход; на 150 онлайн — минус ~37% ядра |
| 2 | P1-1: `playerMove` по изменению, 20 Гц, volatile | минус ~18% ядра, меньше расход батареи, нет «телепортов» после подвисания |
| 3 | P1-2: не слать пустые касты | половина кадров у одиночных и AFK игроков |
| 4 | P1-3: ping 15/25 с + клиентский сторож | обрыв замечается за секунды, а не за 2 минуты |
| 5 | P0-2 / P1-4: карта отдельным кэшируемым ресурсом + очередь входов | рестарт и реконнект перестают быть рывком для всех |
| 6 | P1-5, P1-6, P2-* | шлифовка |
| 7 | P0-3: worker/шардирование | только если после 1–5 онлайн упрётся снова |

Пункты 1–6 сделаны, пункт 7 — нет (после остальных запас есть).

---

## Как воспроизвести замеры

Инструменты лежат в `dev/` и на прод не влияют:

```bash
npm run dev:local                       # тот же сервер, своя in-memory база

# 0. Проверки корректности после оптимизаций
node dev/fanout-check.js                # соседи видят снаряды, дальние — нет
CHROMIUM_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome \
  node dev/smoke.js hero                # реальный клиент + переподключение

# 1. Синтетическая нагрузка: N клиентов, вход, движение, бой, RTT
node dev/loadtest.js 150 40 hub                 # 150 ботов, 40 с, толпа в хабе
PROJ=2 node dev/loadtest.js 150 40 spread       # + 2 снаряда/с на бота (P0-1)
MOVE_HZ=20 node dev/loadtest.js 150 40 spread   # темп playerMove (P1-1)
STAGGER=0 node dev/loadtest.js 150 30 hub       # все входят разом (P0-2)

# 2. Тик мира в изоляции, без сокетов и базы
node dev/roombench.js 300 400 arm

# 3. CPU-профиль живого сервера
node --cpu-prof --cpu-prof-dir=./prof dev/local.js
node dev/profsum.js prof/CPU.*.cpuprofile 30

# 4. Тайминги тика (нужен админ-токен из POST /admin/login)
curl -s localhost:3000/health -H "Authorization: Bearer $TOKEN"
```

`dev/loadtest.js` печатает распределение RTT, худших ботов, темп и объём
`gameState`; `dev/roombench.js` — перцентили `_tick()` отдельно для кастовых и
некастовых тиков. Ключи `MOVE_MODE=legacy|smart` и `IDLE_PCT` переключают бота
между старым и новым поведением клиента — это и есть замер строки «приём
позиций» выше.
