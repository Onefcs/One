# Аудит WebSocket-слоя: насколько оптимизирована мультиплеерная часть

Дата: 2026-08-06 · ветка `claude/game-audit-ping-stability-ewz7tl` (от `eb650b5`)
Предмет: что и как ходит между игроками по сокету — формат, частоты, объём,
надёжность, сглаживание. Это продолжение `AUDIT-PERF.md` (там про нагрузку на
сервер), здесь — про **сам протокол и канал игрока**.

Метод: `dev/netprobe.js` — один инструментированный клиент внутри толпы обычных
ботов, считает трафик по событиям в обе стороны и кадры engine.io под ними.
Стенд тот же: `npm run dev:local`, реальный `server/index.js`.

> **Статус: N1, N3 и N5 сделаны.** Итог — в разделе
> [«Что сделано»](#что-сделано). Коротко: входящий канал −26%, исходящий −50%,
> кадров в секунду вдвое меньше, час игры вместо 72 МБ стоит 53 МБ. N2 и N4
> сознательно отложены (см. там же), N6–N8 не трогались.
>
> Первая версия N3 дала регрессию — у бегущих игроков дёргалась анимация;
> причина найдена, замерена и исправлена, см. «Регрессия и её исправление».

---

## Вердикт

Сетевой слой сделан на уровне выше среднего для браузерной MMO: есть всё, что
обычно и отличает «играбельно» от «резинит» — интерес-менеджмент, дельты,
бинарный кодек, интерполяция по снапшотам, самолечение потерь. Основной резерв
теперь **не в архитектуре, а в упаковке байтов**: примерно половину трафика
можно снять, не меняя ни одной игровой механики.

| Аспект | Оценка | Чем подтверждено |
|---|---|---|
| Интерес-менеджмент (AOI) | **отлично** | 50 соседей стоят почти столько же, сколько 20: 20.1 против 19.0 КБ/с |
| Частоты (симуляция 40 Гц / рассылка 20 Гц / рендер 60 fps) | **отлично** | разделены осознанно, клиент интерполирует |
| Дельты состояния | **хорошо** | шлётся только изменившееся; полные записи — при первом появлении |
| Бинарный кодек | **хорошо, но недоупакован** | 17–18 Б на сущность против ~100 Б в JSON; из них 8 Б — координаты в u32 |
| Надёжность и самолечение | **отлично** | volatile + `enemyResync` + периодический полный рефреш |
| Интерполяция чужих игроков | **хорошо, но буфер тонкий** | 65 мс при интервале 50 мс → запас на джиттер всего ~15 мс |
| Прикладные события боя | **слабо** | `spawnProj` — 133 Б JSON, 28% всего входящего трафика |
| Восходящий канал | **слабо** | `playerMove` — 73 Б JSON ×20/с, 83% исходящего |
| Кадры socket.io | **приемлемо** | 2 WS-кадра на событие; конверт — 9% байт на медиане, см. N4 |
| Адаптация к качеству канала | **нет** | игрок на слабой сети получает тот же поток, что и на оптоволокне |
| Телеметрия сети | **нет** | сервер не знает ни потерь, ни джиттера у клиента |
| Античит по позиции/HP | **компромисс** | позиция клиентская (решение владельца), HP — с лимитом регена |

---

## Что уже сделано правильно

Это не формальность — половина находок «почему лагает» в других играх лечится
именно этими вещами, и здесь они есть.

**1. Область интереса на каждого игрока.** Игроки — в радиусе 600 px и не более
20 ближайших (`PLAYER_CAP`), мобы — в радиусе 1400 px через пространственную
сетку. Проверяется прямо: рост числа соседей с 20 до 50 добавляет к каналу
**5%**, а не 150%.

| Соседей в поле зрения | Входящий канал | В час |
|---|---|---|
| 0 (один, рядом мобы) | 3.2 КБ/с | 11 МБ |
| 5 | 7.2 КБ/с | 25 МБ |
| 20 | 19.0 КБ/с | 65 МБ |
| **50** | **20.1 КБ/с** | 71 МБ |

**2. Дельты вместо состояния.** Моб, который не двигался и не терял HP, в пакет
не попадает (`_pushEnemyEntry`); полная запись — только при первом появлении у
конкретного игрока или раз в минуту для самопочинки.

**3. Бинарный протокол.** `shared/netcodec.js`: сущность — 17–18 байт против
~100 в JSON, ссылки по числовым хэндлам, строки едут только в полных записях.
Замер подтверждает расчёт: пакет одинокого игрока 114 Б = заголовок 12 + 6 мобов
× 17.

**4. Разделение частот.** Мир считается 40 раз в секунду, рассылается 20, клиент
рисует 60 и интерполирует между снапшотами. Это правильная схема, а не
«шлём каждый кадр».

**5. Потери не ломают картину.** Поток мира — `volatile`: на подвисшем канале
пакеты выбрасываются, а не копятся. Это безопасно ровно потому, что поток
самовосстанавливающийся: пропавшего моба клиент запросит сам (`enemyResync`), а
игроков чинит периодический полный рефреш.

**6. Интерполяция по снапшотам, а не «догоняющий лерп».** Чужие игроки рисуются
на 65 мс в прошлом, между двумя реально полученными позициями — линейно, без
предсказания и, значит, без рывков от ошибок предсказания (`js/game.js:694`).
Плюс запасной экспоненциальный лерп, если снапшотов не хватает.

**7. HP не отдан клиенту полностью.** Уменьшение принимается сразу, увеличение —
не быстрее `MAX_HP_REGEN_PER_SEC`, поэтому «hp:max в каждом пакете» не делает
игрока бессмертным (`syncPlayerHp`).

**8. Боевые события адресные.** `enemyHurt`/`enemyKilled` идут только тем, кому
этот моб реально транслируется; снаряды и CC — по радиусу (сделано в `eb650b5`).

---

## Куда уходит канал сейчас

Замер: 20 соседей рядом, каждый стреляет 2 раза в секунду, бой идёт.

**Входящий — 19.0 КБ/с (65 МБ/час):**

| Событие | Раз/с | Байт/с | Средний размер | Доля |
|---|---|---|---|---|
| `gameState` | 19.9 | 12 179 | 614 Б | 64% |
| `spawnProj` | 40.0 | 5 302 | **133 Б** | **28%** |
| `playerHurt` | 0.1 | 7 | 47 Б | <1% |
| кадры engine.io | **79.9/с** | | | |

**Исходящий — 2.0 КБ/с (7 МБ/час):**

| Событие | Раз/с | Байт/с | Средний размер | Доля |
|---|---|---|---|---|
| `playerMove` | 19.9 | 1 448 | **73 Б** | **83%** |
| `spawnProj` | 2.0 | 265 | 133 Б | 15% |

Панель карты добавляет 3.0 КБ/с, пока открыта (было 27.8 до правок в `eb650b5`).

---

## Что осталось оптимизировать

### N1. `spawnProj`/`spawnAoe` — до сих пор JSON, и это 28% канала

Один снаряд — 133 байта:

```json
{"x":1380.4,"y":13380.2,"vx":300,"vy":40,"angle":0.1,
 "size":5,"life":1.8,"color":"#8fbf5a","projType":"arrow"}
```

Из девяти полей реально независимы четыре: `x, y, vx, vy`. `angle` — это
`atan2(vy, vx)`. `size`, `color`, `projType`, `life` полностью определяются
классом стрелявшего, которого получатель и так знает (он есть в потоке игроков).
То есть по проводу достаточно: хэндл стрелка (2 Б) + x,y (4 Б) + vx,vy (4 Б) =
**10–12 байт вместо 133**, в одном бинарном кадре.

Эффект: 5.3 КБ/с → ~0.5 КБ/с. **Минус 25% всего входящего трафика** — самая
дешёвая крупная правка из оставшихся.

### N2. Координаты в кодеке занимают вдвое больше нужного

Раскладка одной записи (`shared/netcodec.js`):

| Поле | Байт | Нужно ли столько |
|---|---|---|
| флаги | 1 | да |
| хэндл | 2 | да |
| **x, y (u32 ×2)** | **8** | **нет** |
| hp (i32) | 4 | не в каждом пакете |
| facing / aggro | 1 | не в каждом пакете |
| atkSeq / таймер атаки | 2 / 1 | не в каждом пакете |
| **итого** | **17–18** | ~8–10 достижимо |

u32 стоит там по честной причине: мир 66 000 px в высоту, в u16 при квантовании
0.5 px влезает только 32 767. Но получателю не нужны абсолютные координаты —
всё, что ему шлют, находится **в пределах его радиуса интереса (1400 px)**.
Смещение относительно позиции получателя — это ±2800 в квантах, то есть i16 с
десятикратным запасом. Минус 4 байта на сущность.

Второе — маска полей. Сейчас hp, facing и atkSeq едут в каждом пакете, хотя
меняются редко: один байт битовой маски и отправка только изменившихся полей
убирают ещё 5–7 байт с типичной записи.

Эффект вместе: запись 17–18 Б → ~9 Б, `gameState` 614 Б → ~330 Б,
12.2 КБ/с → **~6.5 КБ/с**.

### N3. `playerMove` — 73 байта JSON двадцать раз в секунду

```
42["playerMove",{"x":1380.4321,"y":13380.1234,"facing":"front","hp":200}]
```

В бинарном виде это x,y (4 Б) + facing (1 Б) + hp (2 Б) = **7–12 байт**. Это 83%
восходящего канала игрока: 1.45 КБ/с → ~0.25 КБ/с. На мобильном исходящий
трафик дороже входящего по энергии (передатчик), так что эффект больше, чем
выглядит по цифрам. Плюс это разбор JSON на сервере — 1.3% CPU по профилю.

### N4. Каждое бинарное событие — два WebSocket-кадра

socket.io шлёт бинарное событие как JSON-конверт плюс вложение:

```
кадр 1: 451-["gameState",{"_placeholder":true,"num":0}]   ← 46 байт
кадр 2: <бинарные данные>
```

Замер: 79.9 кадров/с на входе при 19.9 событиях `gameState` и 40 `spawnProj`.
Для игрока с плотным окружением конверт — 8% байт, но для одинокого (пакеты по
12 Б) — **79%**, и это всегда лишний кадр и лишний системный вызов на сервере
(`writev` — самая дорогая строка профиля).

Варианты: msgpack-парсер (бинарник кладётся в тот же кадр; меняется одной
опцией, но socket.io-клиент придётся вендорить) либо свой канал поверх
engine.io с однобайтовым тегом события. Второй вариант точнее ложится на то,
что здесь уже и так свой бинарный формат.

#### Перемер: решено не делать

Цифра «79%» относится к пакету в 12 байт, а таких пакетов больше нет. После
того как в `_tick` появилось подавление пустых рассылок (пустой каст не
отправляется вовсе, только хартбит раз в `IDLE_HEARTBEAT_CASTS`), одинокий
игрок перестал получать 20 микропакетов в секунду — именно тех, на которых
конверт и был виден.

Перемер на настоящей `Room` (`node dev/envelope-cost.js 40 600 arm`, 40 игроков,
600 тиков):

| расстановка | подавлено кастов | медиана пакета | доля конверта (медиана) | выигрыш по байтам |
|---|---|---|---|---|
| плотно (разброс 5000px) | 0% | 367 Б | 11% | **6.8%** |
| обычно (разброс 5000–20000px) | 0% | 484 Б | 9% | **5.5%** |
| разрежённо (разброс 20000px) | 58.9% | 68 Б | 41% | **17.6%** |

Ключевое в этой таблице — не сами проценты, а их направление: **выигрыш тем
больше, чем меньше трафика.** Там, где канал реально нагружен (толпа, бой,
плотная расстановка), конверт — 6–11% и экономия ~6%; там, где он 41% — больше
половины кастов и так подавлены, и абсолютные объёмы копеечные. Оптимизация
экономит сильнее всего ровно там, где экономить нечего.

Цена при этом не изменилась: парсер не согласуется при рукопожатии, так что
несовпадение клиента и сервера — это флаг-дей (все открытые вкладки отваливаются
до перезахода), плюс сторонний парсер надо вендорить в бандл и держать в горячем
пути. За 6% байт на нагруженном канале это не окупается.

Остаётся вдвое меньше кадров и системных вызовов — единственный аргумент,
который ещё жив. Но проверить его здесь не на чем: фейковый сокет в стенде не
делает настоящего I/O, а нагрузочный прогон требует базы. Так что «writev —
самая дорогая строка профиля» выше остаётся непроверенным утверждением из
исходного замера, а не основанием для правки.

**Вывод: N4 закрыт как нецелесообразный.** Вернуться к нему имеет смысл только
если профиль на живом сервере покажет, что расход именно на количестве
системных вызовов, а не на байтах.

### N5. Буфер интерполяции — 65 мс при интервале пакетов 50 мс

`_INTERP_MS = 65` (`js/network.js:64`), а снапшоты приходят каждые 50 мс. Значит
запас на опоздание пакета — **15 мс**. Всё, что задержалось сильнее (обычное
дело на мобильной сети, где джиттер 20–50 мс), выводит время отрисовки за
последний известный снапшот: коэффициент упирается в 1, чужой персонаж
замирает, а с приходом пакета прыгает вперёд. Это и есть «дёрганые другие
игроки» при нормальном пинге.

Плюс смещение часов считается **скользящим средним** (`_svrTimeOffset` EMA 0.05)
— среднее по задержкам, а не минимум. Правильнее брать минимум наблюдённой
`serverT − clientT` (наименее задержанная выборка — лучшая оценка часов) и
отдельно держать буфер.

Что делать: буфер 100–120 мс (2–2.5 интервала) либо адаптивный — по измеренному
джиттеру. Цена — +50 мс к тому, насколько «в прошлом» видны чужие; для игры без
хитскана это незаметно, а рывки уходят.

### N6. Нет компенсации задержки в PvP

Сервер проверяет попадание по **текущим** позициям, а атакующий целился в
позицию, которую видел 65 мс назад плюс своя задержка. При 100 мс RTT это
расхождение ~150 мс — на скорости бега это ~40 px. Спасает только очень щедрый
радиус (350 px + размер цели), то есть промахов почти нет, но и «попал ровно
туда, куда целился» тоже не гарантировано. Полноценная компенсация (перемотка
позиций на момент выстрела) — это уже серьёзная работа; для текущего темпа боя
скорее не нужна, но знать про это стоит.

### N7. Поток не подстраивается под качество канала

Игрок на LTE в метро и игрок на оптике получают одинаковые 20 КБ/с. У сервера
уже есть всё для адаптации: `volatile` показывает, когда сокет не успевает
писать, а `_ping`/`_pong` дают RTT. Разумный минимум — снижать частоту кастов
для клиента, у которого копится очередь (20 → 10 Гц), и/или ужимать его радиус
интереса. Сейчас вместо этого пакеты просто выбрасываются.

### N8. Сервер ничего не знает о качестве связи игроков

Ни потерь, ни джиттера, ни размера очереди на отправку. Клиент меряет RTT для
себя и показывает в оверлее, но наверх это не уходит. Одна строка в `/health`
с распределением RTT по подключённым — и жалобу «у меня лагает» станет возможно
проверить, а не обсуждать.

---

## Сколько можно снять суммарно

Все цифры — расчёт от измеренных размеров, для сценария «20 соседей, идёт бой».

| | Сейчас (замер) | После N1–N4 (расчёт) | |
|---|---|---|---|
| Входящий канал | 19.0 КБ/с | **~8.5 КБ/с** | −55% |
| Исходящий канал | 2.0 КБ/с | **~0.5 КБ/с** | −75% |
| Кадров в секунду (вход) | 80 | **~40** | −50% |
| Трафик за час игры | 65 МБ + 7 МБ | **~29 МБ + 2 МБ** | −57% |
| Размер пакета `gameState` | 614 Б | ~330 Б | −46% |

Для мобильного игрока трёхчасовая сессия — это 216 МБ сейчас и ~93 МБ после.
Для сервера это столько же меньше работы `writev`, которая по профилю остаётся
самой дорогой строкой.

Порядок по соотношению «эффект / риск» (N1, N3, N5 — сделаны, см. ниже):

1. **N1** (снаряды в бинарный вид) — минус четверть канала, правка локальная, риск минимальный.
2. **N5** (буфер интерполяции 100–120 мс) — правка в одну константу, лечит рывки у игроков с джиттером.
3. **N3** (`playerMove` в бинарный вид) — минус 75% восходящего и разбор JSON на сервере.
4. **N2** (относительные i16 + маска полей) — самый большой выигрыш по байтам, но это правка обеих сторон кодека: нужны аккуратность и версия протокола.
5. ~~**N4** (один кадр на событие)~~ — **закрыт после перемера**: экономия 6% байт
   на нагруженном канале против флаг-дея и вендоренного парсера. См. разбор в N4.
6. **N7/N8** (адаптация и телеметрия) — не про байты, а про то, чтобы видеть проблему и мочь на неё среагировать.
7. **N6** (компенсация задержки) — только если PvP станет соревновательным.

---

## Что сделано

### Канал одного игрока (20 соседей рядом, идёт бой)

| | Было | Стало | |
|---|---|---|---|
| Входящий | 19.0 КБ/с | **14.1 КБ/с** | −26% |
| Исходящий | 2.0 КБ/с | **1.0 КБ/с** | −50% |
| Кадров WebSocket в секунду | 79.9 | **39.1** | −51% |
| Час игры (вход + выход) | 65.2 + 7.1 = **72 МБ** | 49.6 + 3.7 = **53 МБ** | −26% |
| Снаряд по проводу | 133 Б JSON + свой кадр | **19 Б внутри общего пакета** | −86%, кадров 0 |
| `playerMove` | 73 Б, 20/с | **18 Б, 30/с** (26 с конвертом) | −64% на пакет |
| Пакет `gameState` | 610 Б | 670 Б (везёт ещё и снаряды) | +10% |

### Нагрузка на сервер: сколько стоят визуалы боя

Замерялось как разница «со снарядами» и «без» в одной сессии (абсолютные
значения между сессиями гуляют на ~10%, разница внутри сессии — нет).

| Когда | Цена снарядов, 150 игроков × 2/с |
|---|---|
| до всех правок | **+7.46 с CPU / 20 с** (+67% ко всей работе сервера) |
| после AOI и потолка веера (`eb650b5`) | +1.18 с (+11%) |
| **после доставки внутри каста** | **+0.5 с (+4%)** |
| то же при 200 игроках в давке | **в пределах шума**: 12.39 с против 12.29 с без снарядов |

### Что именно изменено

**N1 — снаряды и AOE едут внутри мирового каста.** Раньше каждый выстрел был
отдельным событием socket.io каждому получателю: ~133 байта JSON в собственном
кадре, до 40 штук в секунду у игрока в бою — 28% всего входящего. Теперь
сервер кладёт снаряд в очередь каждого, кто рядом (`Room.queueProjectile`), и
он уезжает с ближайшим пакетом мира — **19 байт и ноль дополнительных кадров**.

Из девяти полей осталось шесть: `angle` считается из скорости, цвет ужат в три
байта, тип — в один бит. Добавилось поле «возраст»: пакет мира уходит раз в
50 мс, поэтому снаряд может подождать до 50 мс — получатель сдвигает его на
это время вперёд и вычитает из времени жизни, так что визуально он стартует
там, где должен, а не с задержкой.

Формат **дописан в конец пакета**, и это ключевое для деплоя: клиент со старым
кодом дочитывает до списка мобов и просто игнорирует хвост, а новый клиент
против старого сервера видит, что пакет кончился, и считает обе секции
пустыми. Проверено обоими направлениями на реальных буферах.

**N3 — компактный `playerMove`.** Было:

```
42["playerMove",{"x":1380.4321,"y":13380.1234,"facing":"front","hp":200}]   73 Б
42["mv",[2761,26760,0,200]]                                                 26 Б
```

Почти весь объём был упаковкой: длинное имя события, имена полей, направление
словом и четыре знака после запятой у координаты, которую протокол всё равно
округляет до половины пикселя. Бинарный вид был бы ещё меньше, но socket.io
кладёт бинарник **вторым кадром за JSON-конвертом в ~46 байт** — на таком
размере это дороже, чем экономия. Старый обработчик оставлен: клиент, который
не перезагрузил страницу после деплоя, продолжает ходить как ни в чём не бывало.

**N5 — буфер сглаживания 65 → 110 мс.** Снапшоты приходят каждые 50 мс, значит
при 65 мс запас на опоздавший пакет был 15 мс: всё, что задержалось сильнее,
подвешивало чужого персонажа до прихода пакета, а потом он прыгал. Мобильный
джиттер обычно 20–50 мс — отсюда «пинг нормальный, а других дёргает». Теперь
запас — полный лишний интервал. Плата: чужие видны на 45 мс дальше в прошлом,
это ~10 px хода против 350 px радиуса попадания на сервере, то есть на попадания
не влияет.

> Оговорка честности: эффект N5 — расчётный, а не замеренный. Проверено, что
> константа применена и игра работает; воспроизвести реальный джиттер мобильной
> сети на стенде нечем. Судить по нему стоит по жалобам игроков, а не по цифрам
> отсюда.

### Регрессия и её исправление

Первая версия N3 ставила порог отправки позиции в 50 мс, рассуждая так: мир
рассылается 20 раз в секунду, значит чаще слать бессмысленно. Рассуждение
неверное в двух местах, и игроки это увидели сразу — «при беге чужие как будто
поворачиваются назад и обратно».

Во-первых, `netSendMove` вызывается из кадрового цикла, поэтому порог
округляется до кадров: при 60 fps «не чаще 50 мс» — это отправка раз в 66.7 мс,
то есть **15 Гц, ниже частоты рассылки**. Во-вторых, когда отправитель медленнее
рассылки, часть кастов уходит без новой позиции и повторяет предыдущую. У
получателя интерполяция на этом интервале двигаться некуда: чужой персонаж
стоит кадр и прыгает на следующем, а ключ анимации при этом мигает
run → idle → run, и каждый переход сбрасывает `animFrame` в 0 — беговой цикл
начинается заново. На экране это ровно «дёргается и разворачивается».

Замер (`dev/snapshot-check.js`) — доля снапшотов, повторивших предыдущую позицию
у бегущего игрока:

| Темп отправки | Повторов |
|---|---|
| 15 Гц (что реально получалось при 60 fps) | **20.9%** — каждый пятый кадр |
| 20 Гц (номинал того порога) | 1.5% |
| 30 Гц (порог 25 мс, сейчас) | **0%** |
| 40 Гц (как было до N3) | 0% |

Исправлено двумя правками:

* порог вернулся к 25 мс — при 30 и при 60 fps это даёт 30 Гц, с запасом над
  рассылкой. Экономия от N3 при этом почти вся сохранилась: она была не в
  частоте, а в размере пакета (18 Б вместо 73) и в молчании стоящего игрока;
* флаг «движется» у чужого игрока теперь держится 0.2 с, а не читается по
  дельте каждого кадра. Любой единичный интервал без свежих данных — потерянный
  пакет (поток намеренно `volatile`), заминка у отправителя — больше не роняет
  беговую анимацию. Это защита от того же класса артефакта, который частотой
  отправки не лечится в принципе.

### Что осталось и почему

**N2** (относительные i16-координаты и маска полей) и **N4** (один кадр на
событие) дают ещё примерно −40% к пакету мира, но это правка ядра кодека и
транспорта с двух сторон: несовпадение клиента и сервера в момент деплоя
ломает мир у всех. Приёмом «дописать в конец» тут не отделаться — меняется то,
что уже читается. Делать имеет смысл, когда появится причина: рост онлайна
до упора в ядро или жалобы на расход трафика.

Мелочь на будущее: восходящий `spawnProj` (клиент → сервер) остался JSON на
132 байта. Это 2 пакета в секунду с игрока — треть его исходящего, но всего
0.26 КБ/с; тот же приём, что и с `mv`, сократит его до ~30 байт.

### Чем проверено

* `dev/fanout-check.js` — сосед стрелка получает все 10 снарядов и 10 AOE через
  новую доставку, игрок в другой руке — ноль.
* Кодек прогнан на круг: игроки, мобы, снаряды, AOE; отдельно — пакет без
  хвостовых секций (старый сервер) и обрезанный посреди секции: обе ситуации
  декодируются без исключений.
* `dev/snapshot-check.js` — у бегущего игрока ноль повторяющихся снапшотов на
  той частоте, с которой шлёт настоящий клиент.
* `dev/smoke.js` — настоящий клиент в браузере: логин, движение, реконнект,
  карта не перезапрашивается, ошибок в консоли нет.
* Прогон 150 и 200 ботов с боем и снарядами — в логе сервера ноль ошибок
  обработчиков.

---

## Как воспроизвести

```bash
npm run dev:local

# Трафик одного игрока в толпе, с разбивкой по событиям и кадрам
node dev/netprobe.js 20 20 hub

# Как канал масштабируется с числом соседей (проверка AOI)
for n in 0 5 20 50; do node dev/netprobe.js $n 15 hub; done

# С открытой панелью карты
MAPVIEW=1 node dev/netprobe.js 20 15 hub

# Без снарядов — видно чистую стоимость потока мира
PROJ=0 node dev/netprobe.js 20 15 hub
```

`down.framingOverheadPct` в выводе — это и есть доля конверта socket.io (N4),
`down.events[].avgBytes` — размеры, из которых считались оценки выше.
